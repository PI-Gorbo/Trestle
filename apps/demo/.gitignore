node_modules
.nuxt
.output
dist
.data
*.log

# Built by `pnpm build:wasm` from `crates/trestle-wasm`. Regenerated, never committed —
# see README.md for why it is not optional, and the deployment doc for the CI step that
# produces it.
app/lib/trestle-wasm

# wrangler
.wrangler
