// @skip: needs the |> operator (tier 01)
// TARGET: the whole chain on one line still means nested application.
//   x |> f |> g  ==  g(f(x))

let double = (x: Int) => x * 2
let add = (a: Int, b: Int) => a + b

let result = 5 |> add(3) |> double    // double(add(3)(5)) = 16
result