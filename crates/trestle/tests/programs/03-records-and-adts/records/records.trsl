// @skip: needs record types + literals (tier 03) — PROPOSED syntax, not settled
// TARGET: nominal record types with named fields.

type Point = { x: Int, y: Int }

let origin = Point { x: 0, y: 0 }
let p      = Point { x: 3, y: 4 }
