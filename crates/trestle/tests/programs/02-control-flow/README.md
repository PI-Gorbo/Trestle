# Tier 02 — Control flow

Pattern matching. `if` / `if-else` live in `00-basics/conditionals`; this tier is
`match`, which needs more machinery (patterns, exhaustiveness) and pairs naturally
with the ADTs in tier 03.

- `match-expression` — match a value against literal patterns plus a catch-all
  identifier, evaluating to the first arm that fits.

Ignored until `match` / pattern matching lands *(proposed syntax)*.
