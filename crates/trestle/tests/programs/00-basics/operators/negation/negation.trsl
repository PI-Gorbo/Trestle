// TARGET: a leading `-` negates a numeric expression. Distinct from the binary
// subtraction operator (see operators/subtraction). `-7` parses as a unary node
// Unary(Neg, Int(7)) — not a folded Int(-7) — so it generalizes to `-x` and
// `-(a + b)`. Type-checks as Int → Int.

-7
