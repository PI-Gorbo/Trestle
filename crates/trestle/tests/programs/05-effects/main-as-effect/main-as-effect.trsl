// @skip: needs the effect system (tier 05) — PROPOSED syntax, not settled
// TARGET: `main` is itself an effect. The runtime is the boundary where all
// requirements are satisfied and all errors are handled.

let main = effect {
    let name = readLine          // requires a Console capability
    print("hello, " + name)      // requires Console; unmet until the runtime
}
