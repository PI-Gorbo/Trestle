//! Trestle conformance suite.
//!
//! This crate carries no library code of its own — it exists to own the
//! `programs/` corpus of `.trsl` files and the harness in `tests/corpus.rs`
//! that parses them. Cargo requires a target for the package, so this file
//! is intentionally (almost) empty.
